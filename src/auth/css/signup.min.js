import {makeStyles} from "@material-ui/core"

const useStyles = makeStyles((theme) => ({
    root: {
      flexGrow: 1,
      height: "100vh",
      display: "flex",
      // backgroundColor: "#2d2d2d0d",
      justifyContent: "center",
      alignItems: "center",
    },
    paper: {
      padding: "1em",
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: ".5em",
      boxShadow: "4px 5px 8px rgba(0,0,0,0.5)",
    },
    head: {
      color: "#c51162",
    },
    field: {
      display: "grid",
      gridTemplateColumns: "30px 1fr",
      margin: ".5em auto",
      width: "90%",
    },
  }));

  export default useStyles;